import ProfileSection from './ProfileSection';

const Header = () => {
  return (
    <header>
      {/* Your existing header content */}
      <ProfileSection />
    </header>
  );
};

export default Header; 